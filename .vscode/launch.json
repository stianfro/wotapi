{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Package",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "program": "${workspaceFolder}/main.go",
      "hideSystemGoroutines": true,
      "envFile": [
        "${workspaceFolder}/.env",
        "${workspaceFolder}/.env.secret"
      ]
    }
  ]
}